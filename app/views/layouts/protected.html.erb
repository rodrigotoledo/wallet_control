<!DOCTYPE html>
<html>
  <head>
    <title>WalletControl</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="bg-green-500 p-4 flex items-center justify-between">
      <!-- Parte à esquerda do topo -->
      <div class="flex items-center">
        <h1 class="text-white text-xl font-semibold">WalletControl<i class="ml-4 fa-solid fa-wallet  text-white"></i></h1>
      </div>


      <div class="flex items-center space-x-4">
        <%= link_to 'Wallet', root_path, class: 'text-white' %>
        <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'text-white' %>
      </div>
    </div>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
