<!DOCTYPE html>
<html>
  <head>
    <title>WalletControl</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto px-5 flex py-10">
      <div class="flex flex-col text-center w-full">
        <h1 class="font-bold text-4xl text-green-700">WalletControl::You Can Control Your Money</h1>
        <i class="fa-solid fa-wallet fa-10x text-green-700 my-10"></i>
        <%=render 'shared/flash_messages' %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
